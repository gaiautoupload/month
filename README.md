# 月換股儀表板

這個 repository 只放靜態前端網頁，用來顯示每月換股、目前持股、歷史報酬與資產曲線。

不包含後端程式、資料庫或 Discord webhook。

## 使用方式

直接開啟：

```text
dashboard/index.html
```

資料來源是：

```text
dashboard/data.js
```

更新每月換股結果時，只需要替換 `dashboard/data.js`。
